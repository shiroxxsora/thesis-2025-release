# ВАЖНО: Установите PyTorch ПЕРВЫМ под вашу версию CUDA!
# Рекомендуется сначала установить PyTorch по инструкции с https://pytorch.org/get-started/locally/
# Примеры:
# CUDA 12.1:
# pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu121
# CUDA 11.8:
# pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1 --index-url https://download.pytorch.org/whl/cu118
# CPU-only:
# pip install torch==2.7.1 torchvision==0.22.1 torchaudio==2.7.1

# Detectron2 (актуальная версия)
git+https://github.com/facebookresearch/detectron2.git#egg=detectron2

# ВАЖНО: opencv-python НЕ РАБОТАЕТ с numpy>=2.0.0!
# Фиксируем numpy в безопасном диапазоне
numpy>=1.21.0,<2.0.0
scipy==1.15.3
opencv-python==4.10.0

# Геоданные и проекции
GDAL==3.11.0
rasterio==1.4.3
shapely==2.1.1

# Дополнительные зависимости для работы с данными
pycocotools==2.0.8
matplotlib>=3.5.0
Pillow>=8.0.0

# Конфигурации и утилиты (если не подтянутся автоматически)
pyyaml>=5.4.0
tqdm>=4.60.0

geopandas